box: wercker/nodejs
build: 
    steps:
        - npm-install
        - script:
            name: bower-install
            code: ./node_modules/.bin/bower install
        - script:
            name: gulp-build
            code: ./node_modules/.bin/gulp
        - script:
            name: gulp-test
            code: ./node_modules/.bin/gulp test
